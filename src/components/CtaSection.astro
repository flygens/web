---

---

<section class="py-20 md:py-28">
    <div class="container mx-auto px-4">
        <div
            class="relative overflow-hidden rounded-2xl fade-in-up"
            style="animation-delay: 0.3s;"
        >
            <div
                class="absolute inset-0 bg-gradient-to-r from-blue-600 to-purple-600"
            >
            </div>

            <div class="absolute inset-0 opacity-20">
                <div
                    class="absolute left-1/4 top-0 h-40 w-40 rounded-full bg-white/20 blur-3xl"
                >
                </div>
                <div
                    class="absolute bottom-0 right-1/4 h-60 w-60 rounded-full bg-white/20 blur-3xl"
                >
                </div>
            </div>

            <div
                class="relative z-10 flex flex-col items-center justify-between gap-8 p-8 text-center md:p-16 lg:flex-row lg:text-left"
            >
                <div class="max-w-2xl">
                    <h2 class="mb-4 text-3xl font-bold text-white md:text-4xl">
                        Muy pronto tendrás el poder de transformar tus
                        documentos en agentes de IA inteligentes.
                    </h2>
                    <p class="text-lg text-white/80"></p>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* Animaciones */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .fade-in-up {
        animation: fadeInUp 0.7s ease-out forwards;
    }
</style>

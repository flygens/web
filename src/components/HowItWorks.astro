---
import  Cpu from "@lucide/astro/icons/cpu";
import  FileText from "@lucide/astro/icons/file-text";
import  MessageSquare from "@lucide/astro/icons/message-square";

const steps = [
    {
        icon: FileText,
        title: "Subir Documentos",
        description:
            "Sube tus PDFs, documentos de Word o archivos Markdown a Flygens.",
        color: "from-blue-500 to-blue-600",
    },
    {
        icon: Cpu,
        title: "Crea Tu Agente",
        description:
            "Personaliza tu agente de IA con conocimientos y capacidades específicas.",
        color: "from-indigo-500 to-indigo-600",
    },
    {
        icon: MessageSquare,
        title: "Habla Con Él",
        description:
            "Tu agente está listo. Puede responder preguntas, resolver dudas y entregar información útil en tiempo real.",
        color: "from-purple-500 to-purple-600",
    },
];
---

<section id="how-it-works" class="py-20 md:py-28">
    <div class="container mx-auto px-4">
        <div class="mb-16 text-center">
            <h2
                class="mb-4 text-3xl font-bold text-white md:text-4xl fade-in-up"
            >
                Cómo Funciona
            </h2>
            <p class="mx-auto max-w-2xl text-muted-foreground fade-in-up">
                Tres pasos sencillos para transformar tus documentos en agentes
                de IA interactivos.
            </p>
        </div>

        <div class="grid gap-8 md:grid-cols-3">
            {
                steps.map((step, index) => (
                    <div
                        class="relative fade-in-up"
                        style={`animation-delay: ${index * 0.1}s`}
                    >
                        <div class="flex flex-col items-center rounded-xl border border-white/10 bg-black/40 p-6 backdrop-blur-sm">
                            <div
                                class={`mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-gradient-to-r ${step.color}`}
                            >
                                <step.icon class="h-8 w-8 text-white" />
                            </div>
                            <h3 class="mb-2 text-xl font-semibold text-white">
                                {step.title}
                            </h3>
                            <p class="text-center text-muted-foreground">
                                {step.description}
                            </p>

                            {index < steps.length - 1 && (
                                <div class="absolute right-0 top-1/2 hidden -translate-y-1/2 translate-x-1/2 transform text-muted-foreground md:block">
                                    <svg
                                        width="40"
                                        height="40"
                                        viewBox="0 0 24 24"
                                        fill="none"
                                        xmlns="http://www.w3.org/2000/svg"
                                    >
                                        <path
                                            d="M5 12H19M19 12L12 5M19 12L12 19"
                                            stroke="currentColor"
                                            stroke-width="2"
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                        />
                                    </svg>
                                </div>
                            )}
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    /* Animaciones */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .fade-in-up {
        animation: fadeInUp 0.5s ease-out forwards;
    }
</style>

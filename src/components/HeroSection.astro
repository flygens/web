---
import background from "../assets/background.svg";
---

<section
  id="#"
  class="relative overflow-hidden bg-gradient-to-b from-background to-background/80 pt-32 pb-20 md:pt-40 md:pb-32"
>
  {/* Background grid effect */}
  <div
    class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"
  >
  </div>

  {/* Glow effect */}
  <div
    class="absolute left-1/2 top-0 -z-10 h-[600px] w-[600px] -translate-x-1/2 rounded-full bg-purple-500/20 blur-[120px]"
  >
  </div>

  <div class="container relative z-10 mx-auto px-4">
    <div class="grid items-center gap-12 md:grid-cols-2">
      {/* Left Column */}
      <div class="flex flex-col space-y-8 fade-in">
        <div class="space-y-4">
          <div
            class="inline-flex items-center rounded-full border border-purple-200/20 bg-purple-500/10 px-3 py-1 text-sm text-purple-300"
          >
            <span class="mr-1 h-2 w-2 rounded-full bg-purple-400"></span>
            Proyecto solo por diversión, pronto en producción
          </div>
          <h1
            class="text-4xl font-bold tracking-tight text-white md:text-5xl lg:text-6xl"
          >
            Agentes de IA Más Allá de los <br />
            <span
              class="bg-gradient-to-r from-blue-400 to-purple-600 bg-clip-text text-transparent"
            >
              Documentos
            </span>
          </h1>
          <p class="max-w-md text-lg text-muted-foreground">
            Crea agentes de IA conectados a tus documentos y con acceso
            ilimitado a herramientas MCP. Desde RAG hasta datos en tiempo real,
            APIs y más allá.
          </p>
        </div>

        <div
          class="flex flex-col space-y-4 sm:flex-row sm:space-x-4 sm:space-y-0"
        >
        </div>
      </div>

      {/* Right Column */}
      <div class="relative mx-auto w-full max-w-lg scale-up">
        <div class="relative">
          <div
            class="absolute -inset-0.5 rounded-xl bg-gradient-to-r from-blue-500 to-purple-600 opacity-75 blur-lg"
          >
          </div>
          <div
            class="relative overflow-hidden rounded-xl border border-white/10 bg-black/40 p-2 backdrop-blur-sm"
          >
            <img
              src={background.src}
              alt="AI agents connected to documents"
              class="w-full rounded-lg"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animations */
  @keyframes scaleUp {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .scale-up {
    animation: scaleUp 0.5s ease-out 0.2s forwards;
  }

  /* Additional styles */
  .text-muted-foreground {
    color: #ccc;
  }
</style>

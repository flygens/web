---
import Users from "@lucide/astro/icons/users";
import BookOpen from "@lucide/astro/icons/book-open";
import HelpCircle from "@lucide/astro/icons/circle-help";
import Lightbulb from "@lucide/astro/icons/lightbulb";

const useCases = [
    {
        icon: BookOpen,
        title: "Investigadores",
        description:
            "Extrae información de artículos de investigación y documentos académicos.",
        color: "from-blue-500 to-blue-600",
    },
    {
        icon: HelpCircle,
        title: "Equipos de Soporte",
        description:
            "Crea agentes de IA que respondan preguntas desde tu base de conocimientos.",
        color: "from-indigo-500 to-indigo-600",
    },
    {
        icon: Lightbulb,
        title: "Equipos de Producto",
        description:
            "Construye asistentes de IA que comprendan la documentación de tu producto.",
        color: "from-purple-500 to-purple-600",
    },
    {
        icon: Users,
        title: "RRHH y Legal",
        description:
            "Crea agentes que entiendan las políticas y procedimientos de la empresa.",
        color: "from-pink-500 to-pink-600",
    },
];
---

<section id="use-cases" class="py-20 md:py-28">
    <div class="container mx-auto px-4">
        <!-- Título y descripción -->
        <div class="mb-16 text-center">
            <h2
                class="fade-in-up mb-4 text-3xl font-bold text-white md:text-4xl"
            >
                Para Quién Es
            </h2>
            <p class="fade-in-up mx-auto max-w-2xl text-muted-foreground">
                Flygens ayuda a equipos de diversas industrias a aprovechar sus
                documentos con IA
            </p>
        </div>

        <!-- Casos de uso -->
        <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
            {
                useCases.map((useCase, index) => (
                    <div
                        class="group fade-in-up"
                        style={`animation-delay: ${index * 0.1}s`}
                    >
                        <div class="flex h-full flex-col items-center rounded-xl border border-white/10 bg-black/40 p-6 text-center transition-all duration-300 hover:border-white/20 hover:bg-black/60">
                            <div
                                class={`mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-gradient-to-r ${useCase.color} transition-transform duration-300 group-hover:scale-110`}
                            >
                                <useCase.icon class="h-8 w-8 text-white" />
                            </div>
                            <h3 class="mb-2 text-xl font-semibold text-white">
                                {useCase.title}
                            </h3>
                            <p class="text-muted-foreground">
                                {useCase.description}
                            </p>
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    /* Animaciones */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .fade-in-up {
        animation: fadeInUp 0.5s ease-out forwards;
    }
</style>
